<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.ec.mapper.ProductMapper">
  	<select id="findAll" resultType="com.ec.entity.Product">
  		SELECT * FROM products
  	</select>
  	
  	<select id="findById" resultType="Product">
  		SELECT * FROM products WHERE id = #{id}
  	</select>
  	
  	<insert id="insert">
  		INSERT INTO products(name, description, price, stock, created_at, updated_at) VALUES(#{name}, #{description}, #{price}, #{stock}, NOW(), NOW())
  	</insert>
  	
  	<update id="update">
  		UPDATE products SET name = #{name}, description = #{description}, price = #{price}, stock = #{stock}, updated_at = NOW() WHERE id = #{id}
  	</update>
  	
  	<delete id="delete">
  		DELETE FROM products WHERE id = #{id}
  	</delete>
  </mapper>